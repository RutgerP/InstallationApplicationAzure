extends: default

rules:
  # 1000 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 1000
    level: warning

  # don't bother me with this rule
  indentation: disable
